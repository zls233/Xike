# 息刻隐私说明

息刻不要求账户，不连接网络，不包含第三方分析、广告 SDK 或遥测。

任务、专注记录、设置和运行中会话快照仅保存在当前 Mac 的应用容器中。通知权限只用于长休息与恢复提醒；登录项只用于用户主动开启的登录时启动；系统热键通过 macOS Carbon 热键接口注册，不读取键盘输入，也不需要辅助功能权限。

用户可以在“息刻 > 设置 > 数据”中清除专注记录，并可在任务窗口中删除任务。删除任务不会删除既有专注记录中的任务标题快照。

通过 macOS 快捷指令或专注模式配置调用息刻时，系统会把用户选择的任务、目标或节律传给息刻执行，数据仍只保存在本机。
